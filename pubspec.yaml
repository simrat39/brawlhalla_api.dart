name: brawlhalla
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  freezed_annotation:
  json_annotation:
  http:

dev_dependencies:
  build_runner:
  freezed:
  json_serializable:
